First, make sure you're in the right folder(same folder as the .md you are reading).
Then, compile the program with:
```bash
make
```

And to run the program, with your input, use:
```bash
./bin/dsa < sample1.txt
./bin/dsa < sample2.txt
./bin/dsa < sample3.txt
```